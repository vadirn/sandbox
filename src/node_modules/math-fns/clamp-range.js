import { clamp } from 'math-fns/clamp';

export function clampRange([a, b], [min, max]) {
  if (a > b || min > max) {
    throw new Error('Bad range');
  }
  return [clamp(a, min, max), clamp(b, min, max)];
}
